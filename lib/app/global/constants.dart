import 'package:flutter/material.dart';

///
///Color
///

const buttonBgColor = Color(0xff444bff);
const lightAppBarColor = Color(0xffffffff);
const lightAppBarTextColor = Color(0xff000000);
const lightScaffoldBgColor = Color(0xffe9e9ff);
const lightH1TextColor = Color(0xff000000);

///
///Image
///

const appLogo = "assets/images/patika_logo.png";

///
///Padding
///

const Padding paddingS = Padding(padding: EdgeInsets.all(10));
const Padding paddingM = Padding(padding: EdgeInsets.all(20));
const Padding paddingL = Padding(padding: EdgeInsets.all(30));
const Padding paddingXL = Padding(padding: EdgeInsets.all(40));
